# app-soc-pipeline
Multi-OS SOC log ingestion pipeline built in Rust. Collects, parses, and stores system logs (Linux &amp; Windows) into a structured SQLite database for security analysis, detection, and auditing.
